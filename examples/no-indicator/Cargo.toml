[package]
name = "no-indicator"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
furl-core = { path = "../../crates/furl-core" }
tokio = { workspace = true, features = ["rt-multi-thread", "macros"] }
