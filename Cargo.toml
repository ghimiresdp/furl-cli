[workspace]
resolver = "3"
exclude = ["target/", "Cargo.lock", ".github/"]

members = [
    # main crates
    "crates/*",
    # examples
    "examples/*",
]
default-members = ["crates/furl-cli"]

[workspace.dependencies]
tokio = { version = "1.52.3", features = ["rt-multi-thread", "macros"] }
